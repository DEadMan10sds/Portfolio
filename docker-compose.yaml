services:
  portfolio-frontend:
    container_name: portfolio
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5173:80"
    stdin_open: true
    tty: true
    restart: always
